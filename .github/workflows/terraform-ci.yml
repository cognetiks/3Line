name: "3Line CI/CD"

# Validates, plans, and applies Terraform changes on push/PR to main.

on:
  push:
    branches: [ "main" ]
  pull_request:
    branches: [ "main" ]

permissions:
  contents: read
  id-token: write

jobs:
  fmt-check:
    name: "Terraform Format Check"
    runs-on: ubuntu-latest
    steps:
      - name: "Checkout code"
        uses: actions/checkout@v4

      - name: "Setup Terraform"
        uses: hashicorp/setup-terraform@v3
        with:
          terraform_wrapper: false

      - name: "Check formatting"
        working-directory: infrastructure
        run: terraform fmt -check -recursive -diff

  validate:
    name: "Validate (${{ matrix.environment }})"
    runs-on: ubuntu-latest
    strategy:
      fail-fast: false
      matrix:
        include:
          - environment: dev
            aws_region: us-east-1
          - environment: prod
            aws_region: us-east-1
          - environment: dr
            aws_region: us-west-2
    defaults:
      run:
        working-directory: infrastructure/environments/${{ matrix.environment }}
    steps:
      - name: "Checkout code"
        uses: actions/checkout@v4

      - name: "Setup Terraform"
        uses: hashicorp/setup-terraform@v3
        with:
          terraform_wrapper: false

      - name: "Terraform init (no backend/state)"
        run: terraform init -backend=false -input=false

      - name: "Terraform validate"
        run: terraform validate -no-color

  plan:
    name: "Plan (${{ matrix.environment }})"
    runs-on: ubuntu-latest
    needs: validate
    if: ${{ vars.AWS_ROLE_ARN != '' && vars.TF_STATE_BUCKET != '' }}
    strategy:
      fail-fast: false
      matrix:
        include:
          - environment: dev
            aws_region: us-east-1
          - environment: prod
            aws_region: us-east-1
          - environment: dr
            aws_region: us-west-2
    defaults:
      run:
        working-directory: infrastructure/environments/${{ matrix.environment }}
    steps:
      - name: "Checkout code"
        uses: actions/checkout@v4

      - name: "Setup Terraform"
        uses: hashicorp/setup-terraform@v3
        with:
          terraform_wrapper: false

      - name: "Configure AWS credentials"
        uses: aws-actions/configure-aws-credentials@v4
        with:
          role-to-assume: ${{ vars.AWS_ROLE_ARN }}
          aws-region: ${{ matrix.aws_region }}

      - name: "Terraform init (remote state)"
        run: |
          terraform init -input=false \
            -backend-config="bucket=${{ vars.TF_STATE_BUCKET }}" \
            -backend-config="key=${{ matrix.environment }}/terraform.tfstate" \
            -backend-config="region=${{ matrix.aws_region }}" \
            -backend-config="dynamodb_table=${{ vars.TF_STATE_LOCK_TABLE }}"

      - name: "Terraform plan"
        run: terraform plan -input=false -no-color -out=tfplan

      - name: "Upload plan artifact"
        uses: actions/upload-artifact@v4
        with:
          name: tfplan-${{ matrix.environment }}
          path: infrastructure/environments/${{ matrix.environment }}/tfplan
          retention-days: 5

  apply:
    name: "Apply (${{ matrix.environment }})"
    runs-on: ubuntu-latest
    needs: plan
    if: ${{ github.event_name == 'push' && github.ref == 'refs/heads/main' && vars.AWS_ROLE_ARN != '' && vars.TF_STATE_BUCKET != '' }}
    strategy:
      fail-fast: false
      matrix:
        include:
          - environment: dev
            aws_region: us-east-1
          - environment: prod
            aws_region: us-east-1
          - environment: dr
            aws_region: us-west-2

    environment: ${{ matrix.environment }}
    defaults:
      run:
        working-directory: infrastructure/environments/${{ matrix.environment }}
    steps:
      - name: "Checkout code"
        uses: actions/checkout@v4

      - name: "Setup Terraform"
        uses: hashicorp/setup-terraform@v3
        with:
          terraform_wrapper: false

      - name: "Configure AWS credentials"
        uses: aws-actions/configure-aws-credentials@v4
        with:
          role-to-assume: ${{ vars.AWS_ROLE_ARN }}
          aws-region: ${{ matrix.aws_region }}

      - name: "Terraform init (remote state)"
        run: |
          terraform init -input=false \
            -backend-config="bucket=${{ vars.TF_STATE_BUCKET }}" \
            -backend-config="key=${{ matrix.environment }}/terraform.tfstate" \
            -backend-config="region=${{ matrix.aws_region }}" \
            -backend-config="dynamodb_table=${{ vars.TF_STATE_LOCK_TABLE }}"

      - name: "Download plan artifact"
        uses: actions/download-artifact@v4
        with:
          name: tfplan-${{ matrix.environment }}
          path: infrastructure/environments/${{ matrix.environment }}

      - name: "Terraform apply"
        run: terraform apply -input=false -auto-approve tfplan

